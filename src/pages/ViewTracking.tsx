import React from "react";
import {Input} from "antd";

export const ViewTracking: React.FC = () => {
    return (
        <div>
            <Input placeholder="Basic usage" />
        </div>
    );
}